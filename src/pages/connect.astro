---
import Layout from "../layouts/Layout.astro";
---

<Layout
	title="Connect with Us | Acts College SF"
	description="Get connected with Acts College SF! Sign up for updates, ask questions, or learn more about our Christian college fellowship in San Francisco. We'd love to meet you!"
	keywords="join Klesis SF, join Christian fellowship USF, join Christian club SFSU, connect Acts College SF, San Francisco Christian fellowship signup, college ministry SF contact, USF faith community, SFSU Christian group"
>
	<section class="hero">
		<img
			src="/header-banners/Connect Banner.jpg"
			alt="Connect with Acts College SF"
			class="hero-image"
		/>
		<div class="hero-overlay"></div>
		<div class="container">
			<h1>Nice to meet you! 👋</h1>
			<p class="hero-subtitle">
				Sign up for updates, or send us a question or comment below.
			</p>
		</div>
	</section>

	<section class="form-section">
		<div class="container">
			<div class="form-container">
				<iframe
					src="https://docs.google.com/forms/d/e/1FAIpQLSfF4jozD1xm1rmQ7ztVI9SgXf0Xzw_2xGWu-Xu0T-s7w82edQ/viewform?embedded=true&chrome=false&header=false&hl=en&rm=minimal"
					loading="lazy"
					referrerpolicy="no-referrer-when-downgrade"
					frameborder="0"
					marginheight="0"
					marginwidth="0"
				>
					Loading…
				</iframe>
			</div>
		</div>
	</section>

	<section class="info-section">
		<div class="container">
			<div class="info-content">
				<p>
					Acts College SF is a Christian fellowship serving college
					students and young professionals in the San Francisco area.
					We gather weekly for worship, Bible study, and fellowship as
					we grow together in our faith. Whether you attend USF, SFSU, CCSF,
					or any other school in SF, you're welcome to join our community!
				</p>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* Base Container */
	.container {
		max-width: var(--container-max-width);
		margin: 0 auto;
		padding: 0 var(--spacing-sm);
	}

	/* Hero Section */
	.hero {
		position: relative;
		color: white;
		padding: var(--spacing-xl) 0;
		text-align: center;
		overflow: hidden;
		min-height: 60vh;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.hero-image {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		z-index: -2;
	}

	.hero-overlay {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			to bottom,
			rgba(15, 23, 42, 0.4),
			rgba(15, 23, 42, 0.8)
		);
		backdrop-filter: blur(2px);
		z-index: -1;
	}

	.hero h1 {
		font-size: clamp(3rem, 6vw, 4.5rem);
		font-weight: 800;
		margin-bottom: var(--spacing-sm);
		text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
		color: white;
	}

	.hero-subtitle {
		font-size: clamp(1.25rem, 3vw, 1.5rem);
		line-height: 1.6;
		opacity: 0.95;
		max-width: 700px;
		margin: 0 auto;
		font-weight: 500;
		text-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
	}

	/* Form Section */
	.form-section {
		padding: var(--spacing-lg) 0 var(--spacing-xl);
		background: var(--color-bg);
	}

	.form-section .container {
		max-width: 900px;
		margin: 0 auto;
		padding: var(--spacing-sm);
	}

	/* Scaled iframe wrapper */
	.form-container {
		position: relative;
		overflow: hidden;
		/* Adjust height as needed for the form content */
		height: 2000px;
	}

	@media (min-width: 768px) {
		.form-container {
			height: 1600px;
		}
	}

	.form-container iframe {
		display: block;
		width: 100%;
		height: 100%;
		border: 0;
		background: transparent;
	}

	/* Info Section */
	.info-section {
		padding: var(--spacing-xl) 0;
		background: var(--color-bg-alt);
		border-top: 1px solid var(--color-border);
	}

	.info-content {
		text-align: center;
		max-width: 600px;
		margin: 0 auto;
	}

	.info-content p {
		font-size: 1.125rem;
		line-height: 1.7;
		color: var(--color-text-light);
		margin-bottom: var(--spacing-md);
	}

	/* Mobile Styles */
	@media (max-width: 768px) {
		.hero h1 {
			font-size: 2.5rem;
		}

		.form-section,
		.info-section {
			padding: var(--spacing-md) 0;
		}

		.form-container {
			--form-scale: 1;
		}
	}
</style>
